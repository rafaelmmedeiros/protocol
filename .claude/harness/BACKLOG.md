# Harness Backlog — Evolution Mode

The living registry for evolving this repo's harness: `CLAUDE.md`, the `.claude/` skills and
settings, `.mcp.json`, and the build / dev loops. Managed through the `/protocol-harness` skill.

Scope is the whole repo, not one tier: today the MCP servers under `mcps/`, and the backend and
frontend still to come. A pain from any of them goes in the same tables here.

**Rules of this backlog:**

- **Pains require evidence.** Every pain cites a real observed case from work in this repo —
  never "would be nice to have". Without a case, it is an idea.
- **Wins are tracked too.** Knowing what already works prevents regressing it when the harness
  is refactored.
- **Proportional effort.** Small fixes resolve inline and just flip the row to `done`.
  Substantial ones get `.claude/harness/<Name>/DESIGN.md`.
- **Settled rules graduate.** A rule that hardens moves to `CLAUDE.md`; a discrete reusable
  fact goes to memory. This file is deliberation and tracking, not the home of settled rules.

Status values: `open` · `in-progress` · `done` · `parked`.

## Pains

| ID | Evidence (real observed case) | Status | Fix |
|----|-------------------------------|--------|-----|
| P1 | Building the walking skeleton (login across .NET 10, Next 16, Postgres, Docker) there was no workflow to follow. Every step was improvised on the spot: probing the toolchain, settling four conventions `CLAUDE.md` did not answer, reading Next's embedded docs, and inventing a ten-rung verification ladder. Nothing recorded it afterwards, so the next feature would have improvised the same things again. | done | `/protocol-feature` skill written from the observed steps; `CLAUDE.md` gained the application layout, the stack/test commands and the tier invariants. |
| P4 | The E2E suite runs against the compose stack, which is the development database. After a day of work 18 of the 19 rows in `AspNetUsers` were test accounts left behind by Playwright runs. Separately, `docker compose down -v` destroyed that same database once during the walking-skeleton build, one character away from the safe command and documented on the same line as it. | done | Decided: two databases, one for tests and one for development, so nothing ever has a reason to reset the development one. The development database is never destroyed — now standard 14 — and the `-v` flag was removed from the documented commands. The 18 test accounts were deleted. The isolation itself is substantial and gets a `DESIGN.md` before code: the candidates are an ephemeral stack under its own compose project, a second api/web pair pointed at a test database, or tests cleaning up after themselves (weakest — a run that crashes cleans nothing). Also open within it: whether a documented rule is enough, or the volume should be declared external so compose is unable to remove it. Shipped: `docker-compose.test.yml`, a throwaway stack under its own compose project (tmpfs Postgres, no published ports, both suites), and the development `pgdata` declared `external` so compose is unable to remove it. Proof was a count, not a green suite: a full E2E run left the development database at 1 account while its 3 test accounts landed in the test database, and a deliberate `down -v` on the development stack left the volume intact. `.claude/harness/TestIsolation/DESIGN.md`. |
| P3 | `CLAUDE.md` reached 216 lines and the engineer noticed it growing. Most of it was tier detail — .NET commands, Next invariants, MCP build loops — carried into every session regardless of what was being worked on, while the standards that actually bind everything were buried among them. | done | Split into a short root file (product, cross-cutting standards, map) plus `backend/`, `frontend/` and `mcps/CLAUDE.md`. Nested `CLAUDE.md` was chosen over a skill per tier because a skill only loads when invoked, and a convention that depends on being remembered is a convention that gets missed. |
| P5 | The product exists to hold strength-training judgement, and the repo has nowhere to put it. Root `CLAUDE.md` rules that the domain "belongs in a skill … gets written when that feature exists" — which settles when it loads and nothing else: not what a unit of knowledge looks like, not what admits a claim into it, not who challenges one, and not how a deliberate simplification of the literature gets recorded as deliberate rather than as ignorance. The first trainer feature is next and would answer "where does this rep scheme come from" by improvising. First pain here without a lived case; recorded anyway because the cost profile matches every other standard — sourcing a claim while researching it costs minutes, reconstructing the source for a number already baked into a generator means re-deriving every program it produced. | done | Two shapes kept apart — knowledge notes (what the literature supports, with sources, a confidence tier and a stated boundary) and append-only training decision records (what this product does, citing the notes, naming the cost of each simplification) — under a `protocol-training` skill so they load only when training work invokes them. A researcher agent now, an adversarial critic once there is a corpus to critique against (`I3`). Shipped: the `protocol-training` skill with both templates and both gates, the `training-researcher` agent, standard 15 in `CLAUDE.md` (no training judgement in code without a `TD-###` citation), and step 1 of `/protocol-feature` routing training judgements through it. Both directories are deliberately empty — the corpus is a sediment of features built, and there is no feature yet. `.claude/harness/TrainingKnowledge/DESIGN.md`. |
| P2 | `CLAUDE.md` said "there is no build, lint, or test tooling yet" while `backend/` and `frontend/` had full suites. A statement of absence goes stale silently — nothing fails when it stops being true. | done | Rewritten to scope the claim to the MCP servers. The wider lesson (state what exists, not what does not) is not yet a rule; a second case would earn it. |
| P6 | The first real feature set (preferences -> a generated training week) was described in conversation and had nowhere to land: no capability spine (the minimum viable system exists in one paragraph of `CLAUDE.md`), no home for a technical decision that is neither a training judgement nor a harness pain (the frontend proxy architecture survives only as `W2`, which records the lesson and not the decision or its rejected alternative), and no plan or execution record below the level of one change. Recorded before a lived case for the same reason `P5` was: the case is already scheduled and recording it late means a rewrite. | in-progress | Analysed `D:/Projects/mba-ia-greenfield-project` (StreamTube, seven phases, three executed) and took three of its four mechanisms: the capability spine, decision records with Recommendation and Decision as separate fields, and the SI plan plus per-SI `progress.md`. Rejected its anti-drift machine (`sources_mtime`, five planning commands, nine issue categories, six subagents) as sediment from a smaller context window. Proposed: `docs/ROADMAP.md`, `docs/decisions/ADR-###`, `docs/milestones/M<N>-<slug>/{plan,progress}.md`, a new `/protocol-milestone` and a milestone-execution mode inside `/protocol-feature`. Shipped the scaffolding: `docs/ROADMAP.md` (spine + the literal-bullet rule + no status), the `/protocol-milestone` skill (two decision families, the `ADR` template, the plan shape, the `## Open questions` gate), a Milestone mode inside `/protocol-feature` (per-step loop, `progress.md`, three-attempt fix loop, stop between steps), and standard 16 in `CLAUDE.md`. Two design questions settled: `ADR` scope is a grep-able `binds:` frontmatter field rather than an index file, and the roadmap carries intent while `progress.md` owns status. `M1` itself is unwritten — the skill is proven only when a milestone runs through it. `.claude/harness/MilestoneWorkflow/DESIGN.md`. |
| P7 | Three documents drifted from the code and none of them failed anything; all three were found by accident while reading for something else. (1) `frontend/CLAUDE.md` documented `adjustments/ -- Ajustes` in its layout, a route that stopped existing when the fourth section was renamed to `equipment/`. (2) The root `CLAUDE.md` stated "there is no intention of replacing it" about Hevy -- always-loaded text, and false: the intended trajectory ends in a logging surface of our own. (3) Standard 9 read "an exercise is identified by `exercise_template_id`", contradicting standard 8 one line above it and `ADR-002`, which had already decided our own key is the identity. The third was the expensive one: `S1.6` of the M1 plan creates the `exercises` table and would have inherited the contradiction as a primary key, in a migration that is forward-only. `P2` recorded the first case of this class and said a second would earn a rule; this is the third and fourth. | done | Split by failure class, because the three are not one defect: a stale map, an assertion that aged, and an internal contradiction. Only the first and a citation going dangling are machine-checkable, so `scripts/check-docs.mjs` checks those and standard 18 covers the rest. The script's first run found 28 problems of which exactly one was real -- every false positive taught where a document is allowed to name something absent (a generated file, an npm specifier, an HTTP route, a planned server), which narrowed it to layout blocks in a `CLAUDE.md` plus citations under `docs/`. Proven by reintroducing all three drifts on purpose: 3 failures, then clean after reverting. It is rung 1 of the ladder -- the cheapest rung, so it runs first. Standard 18 carries the half no script can reach: say what exists rather than what does not, move the map in the commit that moves the path, and re-read the standards a new `ADR` touches. |

| P8 | Six steps of `M1` ran in one session — five research steps and the exercise catalogue — and produced **47 uncommitted files** against a last commit 24 hours old: 15 knowledge notes, 15 decision records, five backend source files, a migration, a test file and two documents. Nothing in the harness asks for a commit. `/protocol-feature`'s Milestone mode ends each step at "write the `progress.md` entry, then stop", and rung 11 of the ladder (`git status`) only checks that nothing *unwanted* is staged, at the end of the whole milestone. So a twelve-step milestone is designed to land as one commit or none. The cost is already concrete: `TD-008` was superseded by `TD-014` in the same session, and in a single end-of-milestone commit both records appear at once — the fact that `S1.5`'s time arithmetic is what forced the revision is unrecoverable from history. The corpus is append-only by design and git is not mirroring it. | done | Split by where the gap actually was. `/protocol-feature`'s Milestone mode gained a commit step — one step, one commit, `progress.md` entry included, subject naming the step so `git log --oneline` reads as the milestone's spine — plus the rules that keep it honest: stage by path and never `git add -A`, a research step with no code still commits, and a step that cannot be committed cleanly is a finding rather than a reason to batch. Rung 11 now also asserts nothing is left uncommitted. The wider rule graduated to standard 19, because committing as work finishes is not something that should depend on invoking a skill. The distinction that made automatic commits safe: a local commit is reversible and needs no ask, a push is not and always does. The 47 files were reconstructed into seven step-sized commits; the two cumulative files (`SKILL.md`, `progress.md`) could not be split retroactively and landed in one labelled commit, along with two smaller infidelities recorded in its body rather than hidden. Also corrected `/protocol-feature`'s claim that `progress.md` "is the only artifact that survives the session", which this change falsified (standard 18). |
| P9 | Multi-line text edits through Bash failed repeatedly across the `M4` session, in three distinct ways, and two of them corrupted files rather than erroring. (1) Quoted heredocs (`<<'PY'`) aborted with ``unexpected EOF while looking for matching `'`` at least three times — one of them killed the write of 27 catalogue rows mid-step, leaving `ExerciseCatalogue.cs` untouched and the step half-done. (2) A `python - <<'PY'` heredoc **silently collapsed** the two-character sequence `\n` into real newlines, writing a Python file that was invalid and only failed later, at parse time, far from the mistake. (3) `python -c` with an em-dash in the string produced `SyntaxError: unterminated triple-quoted string literal` — the console encoding mangled the character, and every document in this repo uses em-dashes. Earlier in the same session `sed -i "s/\\\\'/'/g"` appended a stray `'` to every line of three C# files. The workaround — `Write` the script to the scratchpad, then `python <file>` — was used roughly fifteen times and never failed once. It also runs against an active instruction to prefer `sed`/heredocs over the dedicated file tools, so the guidance and the observed reliability disagree. | open | — |
| P10 | The eleven-rung ladder went green on `M4` and the generator's session-duration input was inert the whole time. Reproduced by running `WeekGenerator` at 5 days across 30-120 minutes: **50, 60, 75, 90 and 120 minutes all produce the identical week** — 20 slots, sessions of 45/31/25/31/22 min — because `FillSession` stops when each muscle reaches its share of `TD-014`'s weekly target and only consults the clock to reject a slot that will not fit. Time is a ceiling that never binds above ~50 min. Three things failed to notice: the suites (the one test that touches it, `Forty_minutes_and_ninety_minutes_produce_different_weekly_volumes`, runs at **3 days**, the one frequency where 40 min genuinely binds); `check-docs` (`TD-016`'s sentence "slot count is `TD-012`'s minutes arithmetic" is well-formed, resolvable and **false**, which standard 18 already names as the half no script can reach); and the ladder itself, whose rungs assert that code ships, never that an input changes the output. It was found by the engineer reading the Week screen and asking where the numbers came from. | open | — |
| P11 | A feedback session produced roughly eight findings about the product and none of them had a home. The backlog's own scope line says harness — "`CLAUDE.md`, the `.claude/` skills and settings, `.mcp.json`, and the build / dev loops" — so a defect in the generator does not belong here; `docs/decisions/` takes decisions, and these are not decided; `docs/milestones/` has no directory for unplanned work; and `docs/ROADMAP.md` forbids status and treats capability bullets as literal keys, so notes cannot simply be appended to it. The findings were: session duration inert (`P10`), the split table having no way to be chosen, fixed weekday sessions against a user who misses one most weeks, a chronic per-muscle deficit when the same session is always the one skipped, substitution criteria invisible, exercise attributes never reaching the frontend, and per-muscle shortfalls computed by the generator, returned by the API and dropped by the UI. Written down here only because losing them was the alternative. | open | — |
| P13 | Two harness rules collided while writing `M5`'s plan, and one of them had to give. `/protocol-milestone` requires every technical action to cite an `ADR-###`, a `TD-###` or a numbered standard — "an action with no citation is an undeclared decision". It also requires a judgement's research to be its own **earlier step**, so `M5` opens with three research steps producing three training records. But `scripts/check-docs.mjs` validates that every `TD-###` cited under `docs/` **resolves to a record on disk**, and `plan.md` lives under `docs/milestones/`. So the steps consuming those records cannot cite them: the ids do not exist yet, and writing them would fail rung 1 of the ladder — correctly, since that is the dangling-citation class `P7` built the script for. `M1` never hit this because `check-docs` was written during `M1`, at `S1.6`, after its plan. Reserving ids by writing stub records first was rejected on the spot: it would put empty records in an append-only corpus, and `P12`'s gate has just established that a record is committed only once its arithmetic has been measured. | done | The rule was under-specified rather than wrong, so `/protocol-milestone`'s **No orphan action** gained the case it did not cover: an action consuming a record this plan's own research step will produce cites **the producing step** (`S5.2`'s record), and the id is written into `progress.md` when that step lands. The citation is closed by execution rather than by planning, `check-docs` keeps refusing genuinely dangling ids, and the weaker link is bounded to actions whose producing step is named in the same document. `M5`'s plan is the first written under it. |
| P12 | `TD-021` was researched, written, committed (`cbd18bb`) and **superseded within the hour** (`8b199d6`), because nothing measured whether its central number could be implemented as written. It declared a volume band of 6.0 to 8.0 fractional sets. A slot credits 3.0 to its primary muscle, so the band is **narrower than one slot** and no muscle can finish inside it — arithmetic available before the record was written, from constants the record itself cites. Implementation then falsified three further readings in sequence: two-set slots still landed at 10.5 because `TD-006`'s indirect credit arrives from every other slot; bounding the drawn muscle alone, still 10.5; bounding every credited muscle but per session, 9.0, because a later session's deliberately unbounded pass-1 draw lands on top. The measurement that settled it — generate across 2-6 days by 25-120 minutes and read the worst per-muscle figure — took about two minutes, the same probe pattern `W8` already records, and would have run identically the day before. The corpus is append-only (root standard 15's sibling discipline), so the cost is not a rewrite but a **permanent supersession**: `TD-021` stays in the corpus forever, saying something that was never true of any week this system generated. | done | The gate was missing where records are written, not where code is. `/protocol-training`'s **Recording a decision** section gained a fourth gate: a record whose number has checkable arithmetic is measured against the generator before it is committed, and the record states what was measured. Deliberately placed in the skill rather than in `CLAUDE.md` — unlike a convention that must never depend on being remembered, this one applies only inside an activity that always invokes the skill, which is the same reason the other three gates live there. `TD-022` is the first record written under it and carries the grid measurement in its body. |


## Wins

| ID | What worked | Keep because |
|----|-------------|--------------|
| W1 | The verification ladder run in order — compile, unit, integration on a real Postgres, typecheck, frontend unit, stack up, smoke, E2E in Docker, backend suites in Docker, `git status`. | Each rung is cheaper than the one above it, and the top four rungs caught four failures the local run could not see at all. It is the most reusable artifact the skeleton produced. |
| W2 | Following the framework's own agent instructions before writing code. `frontend/AGENTS.md` pointed at `node_modules/next/dist/docs/`; `cookies.md` there is what produced the proxy architecture. | Without it the design would have been browser-to-API with CORS — plausible, and wrong for a cookie session. Framework docs shipped in-repo beat recalled knowledge. |
| W3 | Docker traps commented at the line that would otherwise look arbitrary (Postgres 18's moved volume mount, IPv4-only healthchecks, Testcontainers publishing on the host, `aspnet` shipping without `curl`). | The comment sits where someone would edit and break it, and there is no second document to keep in sync. |
| W4 | Deciding conventions with the engineer up front, then building without further interruption. | Four decisions (auth mechanism, layout, test depth, Docker orchestration) would each have forced a rewrite if discovered mid-build. |
| W6 | Proving the test isolation with a row count on the development database rather than with a green suite, and then running the forbidden `docker compose down -v` against the development stack on purpose. | A passing suite says the tests work, not that they stayed out of the development data — those are different claims and only the count tests the second. And a safety mechanism nobody has ever triggered is a belief: `-v` had to be run to know it was harmless. |
| W7 | The engineer meant to stop the development stack and deleted its containers instead. `docker ps -a` showed nothing; `protocol-project_pgdata` was still there, and `up -d --build` brought the stack back with all 8 accounts intact. | The first time the mechanism was tested by an accident rather than by a drill. `W6` proved `down -v` was harmless on purpose; this was unplanned, and the `external` volume declaration is what made losing containers a non-event. Containers are disposable by construction and the data is not — keep that split whenever a new service is added. |
| W5 | Checking whether a generated `frontend/CLAUDE.md` would compete with the root one by reading Next's own `generate-agent-files.js` and then deleting the file and running `next dev` to see if it came back. | It did not: the generator skips `CLAUDE.md` while `AGENTS.md` holds the managed block. Reading the generator alone would have suggested the file was unavoidable — running the experiment is what settled it. |
| W8 | Answering "why is my week shaped like this" by **running the generator** rather than by reading it — a throwaway probe in the unit project, printing slots and per-session estimates across every supported duration, deleted before the reply was written. | It turned an impression into `45/31/25/31/22`, the engineer's own screen reproduced exactly, and then into the far stronger fact that 50 through 120 minutes are indistinguishable. Reading `FillSession` would have suggested the volume target *could* bind first; only running it showed that above 50 minutes the clock never binds at all. Cost about two minutes against a defect eleven green rungs had missed. |

## Ideas

| ID | Idea | Status | Note |
|----|------|--------|------|
| I1 | Add a prune mode that reviews standing rules and drops the ones that stopped earning their keep. | parked | Deliberately deferred: `CLAUDE.md` is still small enough to read whole. Revisit when it accumulates procedure rather than facts. |
| I3 | A training critic agent: reads a proposed decision or a built feature against the corpus and argues the other side — the claim overreaches, the note under it is `thin`, the simplification costs more than the record admits. | parked | Deferred by design, not by caution: a critic with an empty corpus critiques from model memory, which is exactly what P5's gate forbids. Its trigger is the first training decision the engineer cannot tell is right — which is also the first moment it would have anything to read.
| I2 | An MCP server for Postgres, so the database can be inspected through named read-only tools instead of `psql` in a shell. | open | Raised while looking at the walking skeleton's data. Not yet a pain: `docker compose exec postgres psql` already answers everything, and the skeleton was built without ever needing the database directly. The value when it lands is read-only by construction (the `hevy`/`hevy-write` split), structured output, and named tools rather than recalled `psql` meta-commands — not access itself. Scope it to analysis and debugging, not to tests: a test that reads the database couples to the schema, and neither candidate database suits it anyway (Testcontainers dies with the test process, and the compose one is a dev environment). Trigger it the first time a debugging session actually stumbles over the database. |
