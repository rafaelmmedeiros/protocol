using Microsoft.AspNetCore.Identity;

namespace Protocol.Api.Auth;

/// <summary>
/// The application's user. Empty for now: the walking skeleton only needs identity itself.
/// Profile fields belong here once a feature actually asks for them.
/// </summary>
public sealed class AppUser : IdentityUser
{
}
